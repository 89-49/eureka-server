services:
  eureka-server:
    image: ${AR_IMAGE_PATH}:${IMAGE_TAG:?IMAGE_TAG_REQUIRED}
    container_name: eureka-server
    environment:
      - SPRING_PROFILES_ACTIVE=${EUREKA_PROFILE:?EUREKA_PROFILE_REQUIRED}
      - TZ=Asia/Seoul
    ports:
      - "8761:8761"
    restart: unless-stopped